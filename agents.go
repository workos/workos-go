// Code generated by oagen. DO NOT EDIT.

package workos

import (
	"context"
	"fmt"
	"net/url"
)

// AgentService handles Agents operations.
type AgentService struct {
	client *Client
}

// AgentsUpdateAttemptsParams contains the parameters for UpdateAttempts.
type AgentsUpdateAttemptsParams struct {
	// Type is the operation to perform on the claim attempt. Currently only `link_external_user` is supported.
	Type string `json:"type" url:"-"`
	// ClaimAttemptToken is the token identifying the claim attempt.
	ClaimAttemptToken string `json:"claim_attempt_token" url:"-"`
	// User is the user to attach to the claim attempt, identified by email and external ID.
	User *AgentAdminLinkClaimAttemptToExternalUserRequestUser `json:"user" url:"-"`
	// OrganizationID is the organization to place the agent in. Required when the user belongs to more than one organization.
	OrganizationID *string `json:"organization_id,omitempty" url:"-"`
}

// UpdateAttempts link a claim attempt to an external user
// Link an external user to a claim attempt and retrieve the code needed for the agent to complete the claim. The user is looked up by external ID; if no user exists, one is created. When the user belongs to multiple organizations, an explicit organization must be provided.
func (s *AgentService) UpdateAttempts(ctx context.Context, params *AgentsUpdateAttemptsParams, opts ...RequestOption) (*ClaimViewResponse, error) {
	var result ClaimViewResponse
	_, err := s.client.request(ctx, "PATCH", "/agents/claims/attempts", nil, params, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// AgentsCreateValidateParams contains the parameters for CreateValidate.
type AgentsCreateValidateParams struct {
	// Type is the kind of credential being validated — an agent API key or an agent access token.
	Type AgentAdminValidateCredentialRequestType `json:"type" url:"-"`
	// Credential is the credential value to validate: the API key value for `api_key`, or the access token (JWT) for `access_token`.
	Credential string `json:"credential" url:"-"`
	// Audience is when provided, the access token's `aud` claim is verified against this value. Tokens issued for a different resource are rejected.
	Audience *string `json:"audience,omitempty" url:"-"`
}

// CreateValidate validate an agent credential
// Validate an agent credential — an API key or access token — against the environment of the API key used to authenticate the request. This is a read-only check: it never consumes or mutates the credential.
func (s *AgentService) CreateValidate(ctx context.Context, params *AgentsCreateValidateParams, opts ...RequestOption) (*AgentCredentialValidation, error) {
	var result AgentCredentialValidation
	_, err := s.client.request(ctx, "POST", "/agents/credentials/validate", nil, params, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// GetRegistration get an agent registration
// Retrieve the details of an agent registration by ID. The registration is scoped to the environment of the API key used to authenticate the request.
func (s *AgentService) GetRegistration(ctx context.Context, id string, opts ...RequestOption) (*AgentRegistration, error) {
	var result AgentRegistration
	_, err := s.client.request(ctx, "GET", fmt.Sprintf("/agents/registrations/%s", url.PathEscape(id)), nil, nil, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}
